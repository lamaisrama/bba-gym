AllTrainerDataList=SELECT * FROM(SELECT ROWNUM AS RNUM,A.* FROM(select trainer.t_code,trainer.t_img,member.m_name,center.c_name,m_address_2 from trainer join member using(m_code) join center using(c_code) WHERE member.m_status='N' AND trainer.APPROVAL='Y')A)WHERE RNUM BETWEEN ? AND ?
AllTrainerDataCount=SELECT COUNT(*) FROM TRAINER JOIN MEMBER USING(M_CODE) WHERE member.m_status='N' AND trainer.APPROVAL='Y'
TrainerBadge=SELECT category.category_name FROM T_CATEGORY JOIN CATEGORY USING(CATEGORY_CODE) WHERE t_category.trainer_code=?
searchTrainerData=SELECT * FROM(SELECT ROWNUM AS RNUM,A.* FROM(select trainer.t_code,trainer.t_img,member.m_name,center.c_name,m_address_2 from trainer join member using(m_code) join center using(c_code) where type LIKE ? AND member.m_status='N' AND trainer.APPROVAL='Y')A)WHERE RNUM BETWEEN ? AND ?
serachTrainerAllData=select  count(*) from trainer join member using(m_code) join center using(c_code) where type LIKE ? AND member.m_status='N'AND trainer.APPROVAL='Y'
searchTrainerData2=SELECT * FROM(SELECT ROWNUM AS RNUM,A.* FROM(SELECT TRAINER.T_CODE,TRAINER.T_IMG,MEMBER.M_NAME,CENTER.C_NAME,M_ADDRESS_2 FROM TRAINER JOIN MEMBER USING(M_CODE) JOIN CENTER USING(C_CODE) JOIN T_CATEGORY ON(TRAINER.T_CODE=T_CATEGORY.TRAINER_CODE) JOIN CATEGORY ON(T_CATEGORY.CATEGORY_CODE=CATEGORY.CATEGORY_CODE) WHERE CATEGORY.CATEGORY_NAME LIKE ? AND MEMBER.M_STATUS='N' AND trainer.APPROVAL='Y')A)WHERE RNUM BETWEEN ? AND ?
serachTrainerAllData2=SELECT COUNT(*) FROM CATEGORY JOIN T_CATEGORY USING(CATEGORY_CODE) JOIN TRAINER ON(TRAINER.T_CODE=T_CATEGORY.TRAINER_CODE) WHERE CATEGORY.CATEGORY_NAME LIKE ? AND TRAINER.APPROVAL='Y'
searchCenterName=SELECT C_CODE,C_ADDRESS,C_NAME FROM CENTER WHERE C_NAME LIKE ? AND APPROVAL='Y'
