AllTrainerDataList=SELECT * FROM(SELECT ROWNUM AS RNUM,A.* FROM(select trainer.t_code,trainer.t_img,member.m_name,center.c_name,m_address_2 from trainer join member using(m_code) join center using(c_code))A)WHERE RNUM BETWEEN ? AND ?
AllTrainerDataCount=SELECT COUNT(*) FROM TRAINER
TrainerBadge=SELECT category.category_name FROM T_CATEGORY JOIN CATEGORY USING(CATEGORY_CODE) WHERE t_category.trainer_code=?