searchKeywordPageData=SELECT * FROM (SELECT A.*,ROWNUM AS RNUM FROM(SELECT*FROM center ORDER BY C_ENROLLDATE DESC) A) WHERE RNUM BETWEEN ? AND ?
selectCountCenter=SELECT COUNT(*) FROM CENTER JOIN MEMBER USING(M_CODE) WHERE M_STATUS='N'
updateApproval=UPDATE CENTER SET APPROVAL='Y' WHERE C_CODE=?
selectMemberList=SELECT * FROM (SELECT A.*,ROWNUM AS RNUM FROM(SELECT*FROM MEMBER ORDER BY M_ENROLLDATE DESC) A) WHERE RNUM BETWEEN ? AND ?
selectCountMember=SELECT COUNT(*) FROM MEMBER
updateStatus=UPDATE MEMBER SET M_STATUS='Y' WHERE M_CODE=?