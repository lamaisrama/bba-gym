searchKeywordPageData=SELECT * FROM (SELECT A.*,ROWNUM AS RNUM FROM(SELECT*FROM center ORDER BY C_ENROLLDATE DESC) A) WHERE RNUM BETWEEN ? AND ?
selectCountCenter=SELECT COUNT(*) FROM CENTER JOIN MEMBER USING(M_CODE) WHERE M_STATUS='N'